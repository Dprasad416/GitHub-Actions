name: 
on: push
jobs:
  build: 
    outputs:
      verstion: ${{steps.step1.outputs.verstion}}
    runs-on: ubuntu-latest
    steps:
      - name: step1
        id: step1
        run: echo "verstion=0.1" >> $GITHUB_OUTPUT
  deploy: 
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: step2
        run: echo "the version is ${{needs.build.outputs.verstion}}"
